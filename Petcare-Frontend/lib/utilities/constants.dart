import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Constants {
  final kHintTextStyle = TextStyle(
    color: Colors.white60,
    fontFamily: 'OpenSans',
  );

  final kLabelStyle = TextStyle(
    color: Colors.blue[800],
    fontWeight: FontWeight.w300,
    fontFamily: 'OpenSans',
  );

  static final Color buttonColor = Colors.white;
  static final Color cardColor = Colors.cyan[300];
  static final Color focusColor = Colors.white;
  static final Color primaryColorLight = Colors.red[200];
  static final Color primaryColorDark = Colors.red[800];
  static final Color canvasColor = Colors.white;
  static final Color unselectedWidgetColor = Colors.blue[800];
  static final Color cursorColor = Colors.black;
  static final Color primaryColor = Colors.red;
  static final Color accentColor = Color(0xFF61A4F1);
  static final Color backgroundColor = Colors.red[300];
  static final Color hintColor = Colors.grey[500];
  static final Color panelColor = Colors.red[300];
}
